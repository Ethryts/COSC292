// #pragma once // this will ensure that the header is only loaded once
// you can also use a header guard instead
#ifndef PINGPONG_H // if not defined
#define PINGPONG_H // then define it and do our operations

void ping();
void pong();

#endif 
